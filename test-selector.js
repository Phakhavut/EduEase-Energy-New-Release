import React from 'react';
import { renderToString } from 'react-dom/server';
import { JSDOM } from 'jsdom';
import fs from 'fs';

// To compile the app with Babel, we can just use esbuild or similar.
